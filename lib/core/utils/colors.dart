import 'package:flutter/material.dart';

class AppColors {
  // Primary button color
  static const Color primaryRed = Color(0xFF9B2323);

  // Light text color used on buttons
  static const Color lightText = Color(0xFFF4F4F4);

  // Shadow color (with opacity)
  static const Color black = Color.fromRGBO(0, 0, 0, 0.616);
  static const Color shadow = Color.fromRGBO(31, 30, 30, 0.473);
}
